# Violin Assistant（練琴練習小幫手）開發歷程與交接紀錄

> **這份文件的用途：** 讓使用者換電腦、重新安裝 Claude Code 後，新的 Claude Code session 只要讀這份文件（+ README.md + 原始碼），就能立刻掌握本專案的完整脈絡，不需要重新翻 git log 或重新做健檢。
>
> **這份文件放在 repo 根目錄、會被 git 追蹤**，所以 `git clone https://github.com/blacKgreYcAt/violinassistant.git` 之後就會自動帶著走，不依賴任何本機的 Claude Code 記憶檔案。
>
> 最後更新：2026-07-08

---

## 1. 專案是什麼

**Violin Assistant / 練琴練習小幫手** — 給樂器演奏者（原始定位是小提琴，後改為樂器無關）用的練習輔助 PWA。純前端、無後端、無資料庫，所有資料存在瀏覽器的 `localStorage` + IndexedDB。

- **GitHub：** https://github.com/blacKgreYcAt/violinassistant
- **部署：** Vercel（violinassistant.vercel.app）
- **起源：** Google AI Studio 產生的專案（`metadata.json`、`vite.config.ts` 的 PWA 設定等都是 AI Studio 模板留下的痕跡），`package.json` 內部名稱仍是舊名 `viola-practice-assistant-v2`。
- **本機路徑（2026-07-03 clone 時的機器）：** `C:\Users\imaus\Desktop\20260326 Claude Code Project\violinassistant\`。**這是獨立的 git repo，跟同資料夾裡其他工具（Montbell 圖片整合工具等）完全無關**，換電腦時只要重新 `git clone` 這個 repo 即可，不需要整個 `20260326 Claude Code Project` 資料夾。

### 重要澄清：跟 Gemini API 無關
README.md 目前寫著要設定 `GEMINI_API_KEY`，這是 AI Studio 模板的樣板文字殘留。**實際程式碼裡完全沒有引用這個環境變數，也沒有任何呼叫 Gemini/AI API 的地方。** 所有「智能」功能（如智能翻頁）都是用 `@mediapipe/tasks-vision` 在瀏覽器端做臉部/眼動偵測，不吃任何雲端 AI API。下次維護時看到 README 這段可以直接忽略或修正。

---

## 2. 技術棧

| 類別 | 套件 | 用途 |
|---|---|---|
| 框架 | React 18.3 + TypeScript 5.9 | UI |
| 建置 | Vite 5.4 + `@vitejs/plugin-react` | 開發伺服器/打包 |
| 樣式 | Tailwind CSS 4（`@tailwindcss/vite`）+ `clsx` + `tailwind-merge` | 樣式 |
| PWA | `vite-plugin-pwa` | 離線快取、安裝到主畫面 |
| 動畫 | `motion`（Framer Motion 系列） | UI 過場動畫 |
| 圖示 | `lucide-react` | icon |
| 樂譜 PDF | `pdfjs-dist` | PDF 樂譜渲染 |
| 圖表 | `recharts` | 練習紀錄統計圖 |
| 拖放上傳 | `react-dropzone` | 樂譜上傳 |
| 儲存 | `idb-keyval`（IndexedDB）+ `localStorage`（舊資料/app 標題） | 本機持久化 |
| 電腦視覺 | `@mediapipe/tasks-vision` | 頭部姿態/眨眼偵測（智能翻頁）、調音器音準偵測 |

**啟動方式（正確版，README 沒寫全）：**
```
npm install
npm run dev      # http://0.0.0.0:3000
npm run build    # 產出 dist/
npm run lint     # tsc --noEmit，型別檢查
```
不需要任何 `.env` 設定就能跑起來（`.env.example` 裡的 Google Drive API 那行目前也沒有實際用到，同樣是殘留樣板）。

---

## 3. 專案結構

```
violinassistant/
├── index.html
├── metadata.json          # AI Studio 專案 metadata（app 名稱、camera/microphone 權限宣告）
├── package.json           # name 仍是 viola-practice-assistant-v2，version 2.1.0（落後於 app 內顯示的 v2.2.1）
├── vite.config.ts         # Tailwind + React + PWA plugin 設定
├── src/
│   ├── main.tsx
│   ├── App.tsx            # 根組件：tab 切換（工具/圖書館）、共享節拍器 state、開場動畫
│   ├── index.css
│   ├── lib/
│   │   ├── storage.ts     # 所有資料模型與 IndexedDB/localStorage 存取（見第5節）
│   │   └── utils.ts       # cn() 等小工具
│   └── components/
│       ├── Metronome.tsx        # 節拍器（含拍號、速度漸進模式）
│       ├── Tuner.tsx            # 調音器 + Drone Tone 持續音產生器
│       ├── Timer.tsx            # 練習倒數計時器
│       ├── VideoRecorder.tsx    # 錄影/純錄音、分割畫面、iOS 分享優化
│       ├── ScoreLibrary.tsx     # 樂譜圖書館：資料夾、標籤、搜尋、備份匯出匯入
│       ├── ScoreViewer.tsx      # 樂譜檢視器：PDF/圖片、智能翻頁、畫筆標註、深色模式、分割畫面同步錄影
│       ├── ScoreViewer.backup.tsx  # 舊版備份檔，未被任何地方 import（技術債，見第6節）
│       ├── PracticeDashboard.tsx
│       ├── PracticeGoals.tsx    # 每日/每週練習目標
│       ├── PracticeHistory.tsx  # 近七天練習圖表 + 郵件分享
│       ├── PracticeRoutines.tsx # 自訂練習計畫（暖身/音階/曲目等步驟）
│       ├── RewardCard.tsx       # 音樂集點卡/拼圖徽章遊戲化系統
│       └── UserGuide.tsx        # App 內使用說明 + **內建 changelog（v1.5.0 ~ v2.2.1）**
├── src_backup_2026-03-16/       # 舊版根組件備份（技術債，見第6節）
└── src_backup_20260316/         # 內容幾乎相同的另一份備份（技術債，見第6節）
```

---

## 4. 完整開發歷程

### 4.1 兩份時間軸的落差（先講清楚，避免之後搞混）

這個專案有**兩份記錄開發歷程的來源，日期對不起來**：

1. **`src/components/UserGuide.tsx` 裡手寫的 changelog**（app 內「使用說明→更新日誌」分頁看得到）：版本號 v1.5.0 ~ v2.2.1，標註日期 **2026-02-28 ~ 2026-03-18**。
2. **git commit 歷史**：117 個 commit，最早 **2026-03-13**，最新 **2026-03-18**。

也就是 **changelog 宣稱的最早版本（v1.5.0, 2026-02-28）比 git 上最早的 commit（2026-03-13）還早兩週**。合理推測是這個 app 最初在 Google AI Studio 裡開發、疊代了一段時間（那段沒有進 git），直到 2026-03-13 才把當時的成果一次性推上 GitHub（早期 commit message 都是 AI Studio 自動產生的「Update codes」「Upload codes」「Update package.json」這種無語義訊息），2026-03-13 之後才開始出現正常的 `feat/fix/refactor` 語義化 commit。**這是觀察到的事實、不是猜測後就當真的結論**——如果之後要精確重建早期時間軸，唯一可信的來源是 changelog 裡的敘述文字，git commit 時間對早期版本沒有參考價值。

### 4.2 App 內建 changelog 全文（v1.5.0 → v2.2.1，使用者原話，未改寫）

| 版本 | 日期 | 內容 |
|---|---|---|
| v1.5.0 | 2026-02-28 | 節拍器支援拍號設定（4/4, 3/4）與精準滑桿控制；新增備份/還原（匯出所有樂譜為 JSON） |
| v1.6.0 | 2026-03-05 | 新增練習紀錄與統計（近七天圖表 + 郵件分享）；倒數計時器新增 15/30/45/60 分鐘快選 |
| v1.7.0 | 2026-03-10 | 錄影功能：相機畫面可拖曳至四角並縮小，避免遮擋樂譜 |
| v1.8.0 | 2026-03-13 | 頁尾新增聯繫我們；樂譜上傳影像自動增加對比度並壓縮，解決儲存空間不足 |
| v1.9.0 | 2026-03-15 | **智能翻頁雙模式**：頭部組合動作 / 眨單眼，兩種模式可切換；樂譜清單支援長檔名兩行顯示 |
| v2.0.0 | 2026-03-16 | 一次性大改版：樂譜資料夾+標籤搜尋、自訂練習計畫、**音樂集點卡遊戲化系統**（練習30分鐘=1音符，集滿解鎖拼圖/終極徽章）、畫筆標註+頁面旋轉（永久儲存）、練習筆記、**調音器 + Drone Tone**、純錄音模式；修正 iOS Safari 連續錄影黑畫面 bug |
| v2.1.0 | 2026-03-17 | **分割畫面功能**（樂譜檢視器左右對半切，一邊看譜一邊錄影）、開啟相簿內示範影片與樂譜並排播放；修正舊版練習計畫資料導致白畫面、修正 iPad「符合頁面」模式跑版 |
| v2.1.1 | 2026-03-18 | 使用說明排版優化（智能翻頁/多頁切換改全寬顯示） |
| v2.1.2 | 2026-03-18 | 新增樂譜深色模式（反相顯示，減少長時間看譜眼睛疲勞） |
| v2.2.0 | 2026-03-18 | **曲目熟練度管理**（0-100%，圖書館顯示星等）；**節拍器速度漸進模式**（設定目標速度+增量+間隔小節，自動隨練習提速）；樂譜深色模式 |
| v2.2.1 | 2026-03-18 | 修復分割畫面模式下錄影/相簿影片黑畫面 bug；優化分割畫面大螢幕比例；智能翻頁邏輯修正回標準動作 + 恢復2秒防誤觸冷卻 |

### 4.3 v2.2.1 之後、changelog 未收錄的最新 3 個 commit（2026-03-18，git 上最新但 app 內 changelog 還沒寫）

- `e6a0a89 feat(recorder, score-viewer): Improve video stream handling and face tracking`
- `832c468 feat: Add splash screen with exit animation`（目前看到的開場動畫畫面就是這次加的）
- `844a759 feat(ScoreViewer): Add video playback and file selection`

**這代表 `package.json` 的 version 2.1.0 也是舊的**——app 內 UI 顯示 User Manual v2.2.1，且還有這 3 個更新的 commit 沒被記進 changelog 也沒反映在 package.json。**下次改版時記得同步更新 `package.json` 的 version 欄位跟 `UserGuide.tsx` 的 changelog**，這兩個目前都跟實際狀態脫節。

### 4.4 功能演進脈絡摘要

大致可以看出開發順序是：**基礎工具（節拍器/計時器/錄影/PDF樂譜）→ 練習紀錄與統計 → 練習計畫與遊戲化（集點卡）→ 調音器 → AI 臉部偵測智能翻頁 → 分割畫面同步錄影／示範影片對照 → 熟練度管理與節拍器漸進模式 → 各種 UI/bug 修正收尾**。早期（2026-03-13 前後）花了大量 commit 在 PDF 渲染引擎反覆調整（`b357774`、`2b2f429`、`ae7d790`、`bbd9bfc`、`a4a3c5d`…一系列都是同一天在調 PDF 顯示穩定性），可見 PDF 樂譜顯示曾是早期一個踩坑重點。

---

## 5. 資料模型（`src/lib/storage.ts`）

所有資料透過 `idb-keyval`（IndexedDB）存取，key 前綴統一是 `viola-*-idb`（沿用舊命名，跟目前「樂器無關」的品牌不一致，但不影響功能，改 key 名稱會需要處理遷移，非必要不要動）。

- **`Score`**：樂譜本體。`type: 'file'|'link'`，`data` 可以是單張或多張圖片/PDF；`rotations`/`annotations`/`cropData` 都是**每頁一筆**的陣列；`mastery` 熟練度 0-100；`tempoHistory` 只留最近 30 筆、每天只記當日最高 BPM。
- **`Folder`**：樂譜分類資料夾。
- **`PracticeSession`**：一次練習紀錄（日期、時長、筆記）。
- **`Recording`**：錄影/錄音檔案本體（`Blob` 直接存進 IndexedDB），可附帶 `intonationData`（音準時序資料，目前程式碼有定義型別但需再確認是否已有 UI 消費，見第6節待確認項）。
- **`PracticeRoutine`**：自訂練習計畫（多個步驟，各自有名稱與時長）。
- **`RewardsState` / `RewardResult`**：集點卡遊戲化狀態。規則寫死在 `processPracticeReward()`：**每 1800 秒（30分）= 1 音符，10 音符 = 1 拼圖碎片，36 個碎片（6 類別 × 6 個）集滿解鎖「首席演奏家」終極徽章**。
- **`PracticeGoal`**：每日/每週練習目標（時間/次數/曲目數三種類型）。

`getScores()` 裡有個**資料遷移邏輯**：先讀 IndexedDB，若空才去讀舊版 `localStorage['viola-scores']`（5MB 上限的舊儲存方式）搬過去。**這段遷移邏輯還留著，代表可能還有用戶端存的是舊格式資料，升級套件或重構 storage.ts 時不要刪掉這段，否則舊資料會讀不到。**

---

## 6. 已知技術債 / 待辦事項（2026-07-03 健檢，2026-07-08 記錄）

優先順序由高到低：

1. **`.gitignore` 排除了 `package-lock.json`**：沒鎖版本，不同機器 `npm install` 結果可能不一致，建置不可重現。**建議任何套件升級動作之前先補回並 commit 這個 lockfile。**（有趣的是舊 commit 歷史裡出現過 `2885a07 Update package-lock.json`，代表這個檔案曾經被追蹤過，後來才被加進 `.gitignore` 移除追蹤——原因不明，可留意之後是否有意外把它排除的理由。）
2. **中度安全性漏洞**：esbuild/vite dev server 漏洞（GHSA-67mh-4wv8-2f99）。`npm audit fix --force` 會拉到 vite 8.x（breaking change），需要另外測相容性。
3. **套件普遍落後大版本**：React 18→19、vite 5→8、typescript 5.9→6、pdfjs-dist 4→6、tailwind-merge 2→3、`@vitejs/plugin-react` 4→6、`lucide-react` 0.460→1.x、`vite-plugin-pwa` 0.21→1.x。
4. **殘留備份檔案混在版控裡**：根目錄 `src_backup_2026-03-16/`、`src_backup_20260316/`（兩份內容幾乎相同的舊 App.tsx/index.css/main.tsx），以及 `src/components/ScoreViewer.backup.tsx`。都沒有被任何地方 import，純粹佔空間、容易誤改。建議清掉或至少排除出版控（**清除前先跟使用者確認，不要自作主張刪除**）。
5. **`README.md` 是 AI Studio 預設樣板**："Run and deploy your AI Studio app" 文字未改，GEMINI_API_KEY 說明與實際程式碼不符（見第1節）。
6. **`package.json` version（2.1.0）落後於 app 內實際版本（UI 顯示 v2.2.1，且還有 3 個更新 commit 未計入 changelog）**：見第4.3節。
7. **打包體積偏大**：build 後主 JS chunk 850KB（gzip 253KB），vite 有跳出「chunk 太大」警告，可考慮之後用動態 `import()` 做 code-splitting，非急迫。
8. **命名不一致**：`storage.ts` 裡所有 IndexedDB key 都還是 `viola-*` 前綴（沿用「viola 中提琴」舊品牌），但 UI 品牌已經改成樂器無關的「練琴練習小幫手」。純粹是命名debt，不影響功能，不建議為了美觀去動 key 名稱（會牽涉資料遷移風險）。

沒有發現 TODO/FIXME 標記，`tsc --noEmit` 與 `npm run build` 目前都能跑過。

---

## 7. 給下一個 Claude Code Session 的接手指南

如果你是換了電腦、重新安裝 Claude Code 後接手這個專案的 session，建議順序：

1. `git clone https://github.com/blacKgreYcAt/violinassistant.git`
2. 讀這份文件（你正在讀的這份）+ `README.md`
3. `npm install && npm run lint && npm run build` 確認環境正常
4. 跟使用者確認第 6 節「已知技術債」目前哪幾項已經處理過、哪幾項還沒（**這份文件寫的是 2026-07 當時的狀態，套件版本號、漏洞清單會隨時間過期，重新 `npm outdated`/`npm audit` 驗證一次比直接相信這份文件更可靠**）
5. 若要接續開發新功能，先看第4節的功能演進脈絡與第5節資料模型，避免重複實作已有的機制（例如遊戲化集點卡規則、熟練度欄位、tempoHistory 只留30筆等既有設計）

**維護這份文件的方式：** 之後每次做完一輪明顯的功能開發或版本發布，順手在第4節加一段、第6節更新待辦狀態即可，不需要另外開新檔案。這份文件會隨 git 一起走，換機器/重裝 Claude Code 都不會遺失。
